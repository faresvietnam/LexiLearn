# Phase 3 FSRS Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or **superpowers:executing-plans** to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Integrate automatic FSRS scheduling into the persisted learning flow without changing the learner interaction sequence.

**Architecture:** A pure rating module converts the complete attempt path to an FSRS rating. A small scheduler adapter owns the `ts-fsrs` API and maps persisted rows to/from the scheduler state. The learning session orchestrator calls both after Answer Review, then saves the card state through the existing repository boundary.

**Tech Stack:** React/Vite, TypeScript, Vitest, Supabase PostgreSQL/RLS, `ts-fsrs`.

## Global Constraints

- `desired_retention = 0.90`.
- Preserve Check → Retry → Answer Review → Continue.
- Retry failures remain `Again` evidence even when the retry succeeds.
- UI never computes intervals; FSRS owns due dates and state.
- No Gemini/Vercel Function, CSV, moderation transaction, calibration, or Stage 4 in these tasks.
- Admin remains an in-app role-gated tab; no `/admin` route.

### Task 1: Automatic rating module

**Files:** Create `src/features/scheduling/automaticRating.ts` and test; use existing `QuestionType` and attempt types.

- [ ] Write RED tests for all rating rules and speed boundaries.
- [ ] Implement baselines and `deriveAutomaticRating(input)` as a pure function.
- [ ] Verify focused tests, lint, and commit `feat: add automatic fsrs rating mapping`.

### Task 2: FSRS adapter and card schema

**Files:** Create `src/features/scheduling/fsrsScheduler.ts` and test; modify package lock; create a forward Supabase migration and update `sessionRepository`/persistence types.

- [ ] Write RED adapter tests for new, review, Again, Good, and due/retrievability output.
- [ ] Install the pinned `ts-fsrs` dependency and add FSRS-compatible card fields/migration without rewriting applied migrations.
- [ ] Implement row/state mapping and `scheduleCard(card, rating, reviewedAt)`.
- [ ] Apply migration, run advisors, focused tests, and commit `feat: add fsrs scheduler persistence`.

### Task 3: Learning-session integration and schedule UI

**Files:** Modify `App.tsx`, `LearningSessionView.tsx`, `WordDetailModal.tsx`, `sessionRepository.ts`; add integration tests.

- [ ] Write RED tests proving a completed retry sends `Again` evidence and Answer Review renders predicted recall/due time.
- [ ] Call the rating and scheduler after the existing final answer-review transition; persist card state and keep local flow alive on write failure.
- [ ] Add relative due-time formatting and schedule display.
- [ ] Run full suite/lint/build and commit `feat: integrate fsrs into learning flow`.

### Task 4: Phase 3 verification gate

- [ ] Run all tests, lint, build, migration checks, and Supabase security/performance advisors.
- [ ] Perform one authenticated manual review with a retry and one direct recall answer.
- [ ] Commit any verification-only docs and mark Phase 3 FSRS scope complete before planning Gemini/Vercel.

### Task 5: Personal Gemini key and direct browser Auto-Fill

**Files:** Create settings UI/repository tests; create a forward migration adding an owner-only `user_settings.gemini_api_key` field; update the Auto-Fill flow.

- [ ] Add RED tests for owner-only key read/write, save/remove behavior, missing-key fallback, and request construction without logging the key.
- [ ] Add the migration and RLS policy/grant using the existing `user_settings` ownership boundary. Do not expose the key in Admin/user-directory queries.
- [ ] Implement browser-direct Gemini requests using the saved personal key, with quota/HTTP/error feedback and a manual-entry fallback. Do not add a Vercel proxy for this personal-key mode.
- [ ] Verify full tests, lint, build, migration checks, and document the browser key exposure trade-off.

### Task 6: Cloudflare R2 image storage

**Files:** Create a Vercel Function for presigned upload URLs and its tests; add R2/Supabase metadata migration and repository/UI tests; update `.env.example` and deployment docs.

- [ ] Add RED tests for authenticated ownership, allowed image MIME types, size limit, scoped object keys, and rejected anonymous requests.
- [ ] Implement the Vercel Function using server-only R2 S3 credentials; return a short-lived presigned PUT URL and object metadata. Never expose R2 credentials to the browser.
- [ ] Upload from the browser to R2, then persist only the object key/URL in Supabase; handle failed upload without mutating the word form.
- [ ] Verify Vercel Hobby-compatible execution, full tests/lint/build, and document required R2/Vercel variables.
